# SATLIB archive location

Download the original SATLIB `.tar.gz` archives into this directory before
running `../screen_satlib.py` with its default settings. These third-party
benchmark archives are intentionally excluded from the repository.
